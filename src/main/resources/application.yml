management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  application:
    name: SB-JWT-Author_Authent
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/userjwt
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
